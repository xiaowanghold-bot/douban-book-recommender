[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
target-version = "py312"
exclude = [
    ".git",
    ".pytest_cache",
    ".ruff_cache",
    "venv",
    "data",
    "notebooks",
]

[tool.ruff.lint]
select = ["E", "F"]
ignore = ["E501"]
